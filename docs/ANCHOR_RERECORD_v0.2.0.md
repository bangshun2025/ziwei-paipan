# L1 锚点重录说明（v0.2.0 口径）

> 执行日：2026-09-11 ｜ 前置裁决：v0.2.0 宪法口径（立春换年 / 节气十二节轴；ALGORITHM §2.3/§2.5） ｜ 任务：L-02
> 结论：L1 锚点回归 **1139/1139 全绿**（重录前 1074/1139，65 条失配）。原值保全：git 历史（重录前 = c3a2753）+ 本机备份 `/tmp/zw62/anchors_backup_20260911`。

## 一、背景

锚点 `tests/anchors/a01-a12.json` 首版产生于 v0.1.0 口径（年界=正月初一、闰月十五分界、月轴=农历月），来源 iztro@2.6.0 bySolar 实测。v0.2.0 口径（commit 4b3e05a 起）将年界改为**立春**、月轴改为**节气十二节**、闰月退出安星——非边界盘两者结果相同（a01/a02/a03/a05/a06/a07/a10/a11/a12 全过，未改动），边界盘产生系统性差异。

重录前全量回归：断言 1139，通过 1074，**失败 65**（全部集中于 a04=32、a08=32、a09=1）；另有 24 条观察项（宫内顺序 / mutagenMap 登记，不判失败）。

## 二、差异分类与逐盘核对（65 条 → 全部为口径预期，无真 bug）

### a04（1990-1-1 午时 男）—— 变更 33 处（32 内容 + 1 顺序归一）

月轴 农历腊月 → 节气子月（1/1 在大雪 12/7 与小寒 1/5 之间）的连锁：

| 类别 | 变更 | 手算核对 |
|---|---|---|
| 月柱 | 丁丑 → 丙子 | 五虎遁「甲己之年丙作首」：己年子月=丙子 ✓（丑月=丁丑 即腊月轴） |
| 命宫 / 身宫 | 未 → 午 | 命宫=寅+(月−1)−时序：月12→未 / 月11→午 ✓；身宫=寅+(月−1)+时序：月12→未 / 月11→午 ✓ |
| 命主 | 武曲 → 破军 | 未宫命主武曲、午宫命主破军（命宫位移的必然结果） |
| 十二宫名 / 大限区间 | 全盘沿生年十二宫环位移 1 位 | 命宫位移的直接结果（大限随命宫重排：5–14↔115–124 闭环） |
| 辅星 | 左辅 卯→寅、右弼 亥→子 | 左辅=辰+(月−1)顺：月12→卯、月11→寅 ✓；右弼=戌−(月−1)逆：月12→亥、月11→子 ✓ |
| 宫内顺序 | 巳宫 minor 顺序归一（[地空,地劫,陀罗] → [陀罗,地劫,地空]） | 仅顺序非集合；原为 iztro 导出顺序，重录归一为现行渲染顺序（此前以观察项存在） |

### a08（1800-1-1 辰时 男）—— 变更 36 处（32 内容 + 4 顺序归一）

与 a04 同构：月柱 丁丑→丙子；命宫 酉→申（寅+(12−1)−4=酉 → 寅+(11−1)−4=申 ✓）；身宫 巳→辰；命主 文曲→廉贞；宫名/大限全盘位移；辅弼移位（右弼 亥→子）；另 4 处宫内顺序归一（巳/午/未/申）。

### a09（2200-12-31 未时 女）—— 1 处（语义变更）

2200 超出实现支持范围：节气表 1000–2100（`js/algorithm.js` L288 显式报错）。由「全字段快照」改为**边界保护期望**：

```json
"assert": { "expectError": "节气表超出支持范围(1000-2100)" }
```

（期望显式报错、禁止静默算错；运行器已支持 expectError 型锚点。）

> 探测佐证：2100-12-31 可算（上限内）、2101-1-1 报错——a09 语义即锁定该上限行为。上限内邻近全字段锚点（如 2100-12-31）如需覆盖，可后续以 iztro 环境（/tmp/refz）补一盘，未纳入本轮范围。
> 备注：历法换算表范围 1800–2200 仅作用于农历换算路径；本盘报错源为节气表。

## 三、方法与工具

- 工具：`tests/tools/regen_anchors.js` —— 按现行代码重算 assert，**只改值不改结构**（键集合与原锚点完全一致）。脚本头已写明使用须知，禁止 `all` / 禁止 CI 使用。
- 运行器：`tests/run_anchor_tests.js` 新增 expectError 分支（边界保护型锚点）。
- 命令与产物：`node tests/tools/regen_anchors.js a04 a08`（差异账全文留档本机 `/tmp/zw62/regen_run.txt`；a04 33 处 / a08 36 处）。

## 四、验证链（重录后）

1. `node tests/run_anchor_tests.js` → **1139/1139 ALL PASS，退出码 0**（24 条观察项保留：顺序 / mutagenMap 未登记，均为非失败信息）。
2. `node tests/run_case_tests.js` → 45/45 ALL PASS。
3. `bash scripts/check-release.sh` → 全绿（v0.5.0）。

## 五、红线（再次重录前必读）

锚点是验收基准，不是实现快照——重录仅可在**口径裁决变更**时执行，且必须：
① 先逐条核对差异性质（口径类 / 展示类 / 真 bug 分类；发现真 bug 先修复再录，禁止用重录掩盖）；
② 走 `tests/tools/regen_anchors.js`（只改值不改结构）；
③ 旧值保全（git 历史 / 备份目录）；
④ 更新本文档同类说明 + CHANGELOG 记录。
